{% extends 'base.html.twig' %}

{% block title %}test-de-page - ZeTilt CMS{% endblock %}

{% block body %}
<div class="bg-white py-24 sm:py-32">
    <div class="mx-auto max-w-7xl px-6 lg:px-8">
        <div class="mx-auto max-w-2xl lg:mx-0">
            <h1 class="text-3xl font-bold tracking-tight text-gray-900 sm:text-4xl">
                test-de-page
            </h1>

            {# Add your page content here #}
            <div class="mt-8 prose prose-lg max-w-none">
                <p class="text-xl leading-8 text-gray-600">
                    Welcome to the test-de-page page. Edit this template at <code>templates/pages/test-de-page.html.twig</code> to customize the content.
                </p>

                {# Example sections you can add:

                <section class="mt-16">
                    <h2>{{ 'demo.section_title'|trans({}, 'pages') }}</h2>
                    <p>{{ 'demo.section_content'|trans({}, 'pages') }}</p>
                </section>

                <div class="mt-16 grid grid-cols-1 gap-8 md:grid-cols-2">
                    <div class="bg-gray-50 p-6 rounded-lg">
                        <h3>{{ 'demo.feature_1'|trans({}, 'pages') }}</h3>
                        <p>{{ 'demo.description'|trans({}, 'pages') }}</p>
                    </div>
                    <div class="bg-gray-50 p-6 rounded-lg">
                        <h3>{{ 'demo.feature_2'|trans({}, 'pages') }}</h3>
                        <p>{{ 'demo.description'|trans({}, 'pages') }}</p>
                    </div>
                </div>

                #}
            </div>
        </div>
    </div>
</div>
{% endblock %}